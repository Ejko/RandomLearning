int fib(int n){
    if(n==1||n==2){
        return 1;
    }
    else{
        int result=fib(n-1)+fib(n-2)
        return result
    }
}

//println "Please enter the position up to which Fib will be calculated: "
//int input=Integer.parseInt(System.console().readLine())
println fib(10)
